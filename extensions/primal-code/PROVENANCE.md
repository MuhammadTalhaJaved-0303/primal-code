# Provenance

This built-in is a **build artifact**, not source. Do not edit `dist/` here.

| | |
|---|---|
| Source repo | https://github.com/MuhammadTalhaJaved-0303/Tendra |
| Path | `extension/` |
| Version | 0.2.0 |
| Built from commit | `9821562` |

The source deliberately lives in one place. Vendoring a second copy into this
fork would let the two drift, and this fork is what ships.

To update: build `extension/` in the source repo, copy `dist/`, `media/`,
`package.json` and `README.md` here, and update the commit above.
